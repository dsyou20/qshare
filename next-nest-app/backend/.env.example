# Backend Port
PORT=3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# API Prefix
API_PREFIX=/api

# Environment
NODE_ENV=development

# Database (예시)
DATABASE_URL="postgresql://postgres:password@localhost:5432/qshare?schema=public"

# Security
CORS_ENABLED=true
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=debug

# Cache
CACHE_TTL=3600

# JWT 설정
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=1d

# BCRYPT_SALT_ROUNDS
BCRYPT_SALT_ROUNDS=10

# Note: 
# 환경 설정 파일 우선순위
# 1. .env.local
# 2. .env.development.local (개발 환경)
#    .env.production.local (프로덕션 환경)
# 3. .env.development (개발 환경)
#    .env.production (프로덕션 환경)
# 4. .env

# 사용 방법:
# 1. 이 파일을 복사하여 .env.development.local 또는 .env.production.local 생성
# 2. 환경에 맞는 값으로 수정하여 사용
# 3. .env*.local 파일들은 .gitignore에 포함되어 있음 